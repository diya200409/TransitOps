[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["python -m pip install --upgrade pip", "pip install -r transitops-backend/requirements.txt"]

[phases.build]
cmds = []

[start]
cmd = "cd transitops-backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
